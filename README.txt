probably going to want to create a virtual environment 

'python -m venv <name>'
'source <name>/bin/activate'

then install requirements into the virtual environment

'pip install -r requirements.txt'